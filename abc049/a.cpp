#include <bits/stdc++.h>
using namespace std;

int main() {
    char c;
    cin >> c;

    if (c == 'a' || c == 'i' || c == 'u' || c == 'e' || c == 'o') {
        puts("vowel");
        return 0;
    }
    puts("consonant");
}
